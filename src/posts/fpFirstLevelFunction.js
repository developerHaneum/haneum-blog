import { maker } from '../components/maker.js';

const contentMsg = `
    <h1>JavaScript functional programming first level function</h1>
    <p>함수가 값으로 다뤄질 수 있음을 뜻합니다.</p>
    <p>JavaScript에서는 함수는 일급입니다.</p>
`;
export const fpFirstLevelFunction = () => maker(contentMsg);
